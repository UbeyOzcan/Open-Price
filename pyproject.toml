[project]
name = "open-price"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "UbeyOzcan", email = "ozcanubey@outlook.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
open-price = "open_price:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
